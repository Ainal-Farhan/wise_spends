/// File category enum — stored as text in [FileStorageTable].
enum FileCategory { profileImage, document, receipt, attachment, backup, other }

/// File storage status enum — stored as text in [FileStorageTable].
enum FileStorageStatus { active, deleted, pending, failed }
