import 'package:flutter/material.dart';

class ThEditMoneyStorageForm extends StatelessWidget {
  const ThEditMoneyStorageForm({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Edit Money Storage Form'),
    );
  }
}