import 'package:flutter/material.dart';

class ThEditSavingForm extends StatelessWidget {
  const ThEditSavingForm({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Edit Saving Form'),
    );
  }
}