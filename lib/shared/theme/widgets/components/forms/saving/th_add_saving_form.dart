import 'package:flutter/material.dart';

class ThAddSavingForm extends StatelessWidget {
  const ThAddSavingForm({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Add Saving Form'),
    );
  }
}