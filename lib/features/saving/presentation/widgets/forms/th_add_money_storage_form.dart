import 'package:flutter/material.dart';

class ThAddMoneyStorageForm extends StatelessWidget {
  const ThAddMoneyStorageForm({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Add Money Storage Form'),
    );
  }
}